{
  "name": "nirogitanman",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:web": "npm run dev --prefix apps/web",
    "dev:app": "npm run dev --prefix apps/app",
    "dev:partner": "npm run dev --prefix apps/partner",
    "dev:admin": "npm run dev --prefix apps/admin",
    "build:web": "npm run build --prefix apps/web"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "^5.5.3"
  }
}
